# Live Demo - [FlosLabs](https://floslab.de/)

# Debug locally
Docker up `docker compose -f compose.dev.yaml up -d`

Docker down
`docker compose -f compose.dev.yaml down`

`cd ./frontend && npm run dev`


# Deploying 

`docker compose up --build`
